// Tells the manager where on screen to place a prompt: on the left side, center, or right side. Each value corresponds to a different RectTransform anchor in the UI canvas.

namespace MHZE.InputPromptSystem
{
public enum InputPromptLocation
{
    Left,
    Center,
    Right
}
}